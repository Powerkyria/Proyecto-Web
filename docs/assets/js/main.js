"use strict";const user=document.querySelectorAll(".flex");function animation(){let e=document.documentElement.scrollTop;for(let t=0;t<user.length;t++){user[t].offsetTop<e&&(user[t].style.opacity=1,user[t].classList.add("userOdd"))}}window.addEventListener("scroll",animation);